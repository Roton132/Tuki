# number-tool

A simple Python tool to reverse line order of numbers from a text file.

## 🔧 Features
- Reverse numbers line by line (top → bottom)
- Works with large number lists
- Simple & clean Python script
- No extra libraries required

## 📁 Files
- `main.py` → Main script
- `Number_List.txt` → Input file (one number per line)
- `reversed_Number_List.txt` → Output file (auto generated)

## ▶️ Usage
```bash
python main.py
